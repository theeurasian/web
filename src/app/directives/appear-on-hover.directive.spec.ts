import { AppearOnHoverDirective } from './appear-on-hover.directive';

describe('AppearOnHoverDirective', () => {
  it('should create an instance', () => {
    // @ts-ignore
    const directive = new AppearOnHoverDirective();
    expect(directive).toBeTruthy();
  });
});
