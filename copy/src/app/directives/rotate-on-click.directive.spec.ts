import { RotateOnClickDirective } from './rotate-on-click.directive';

describe('RotateOnClickDirective', () => {
  it('should create an instance', () => {
    const directive = new RotateOnClickDirective();
    expect(directive).toBeTruthy();
  });
});
