import { AppearOnClickDirective } from './appear-on-click.directive';

describe('AppearOnClickDirective', () => {
  it('should create an instance', () => {
    const directive = new AppearOnClickDirective();
    expect(directive).toBeTruthy();
  });
});
