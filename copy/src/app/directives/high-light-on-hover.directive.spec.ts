import { HighLightOnHoverDirective } from './high-light-on-hover.directive';

describe('HighLightOnHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new HighLightOnHoverDirective();
    expect(directive).toBeTruthy();
  });
});
